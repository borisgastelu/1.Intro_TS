namespace tipo_datos {

    // any
    let data: any;          // acepta cualquier tipo de dato
    let info;

    // number
    let edad: number = 21;
    let calificacion = 18.5;

    // boolean
    let esMax: boolean = true;
    let esMin: boolean = false;

    // string
    let texto = 'Saludos';

    // null
    let animal = null;
    let ventas: any = null;

    // arrays
    let nombres: string[] = ['Laura', 'Jose', "Raul"];
    console.log(nombres.length);        // 3

    // undefined
    let cantidad: number;       // undefined
    let company = undefined;

}
