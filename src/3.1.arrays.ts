// Arrays, coleccion de elementos ordenados secuencialmente
namespace arrays {

    let nombres: string[] = ['Juan', 'Maria', 'Pedro'];
    console.log(nombres);

    let numeros: number[] = [];  // Declaracion
    numeros[0] = 20;
    console.log(numeros);

    let lista: any[] = ['Hola', true, 50, { nombre: 'Juan', edad: 20 }];
    console.log(lista);

}
